name: CI/CD with Minikube, Docker Hub, and GitHub Actions

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

env:
  DOCKER_USERNAME: ${{ secrets.DOCKER_USERNAME }}
  DOCKER_PASSWORD: ${{ secrets.DOCKER_PASSWORD }}
  DOCKER_IMAGE_NAME: my-node-app
  # FIX: Concatenate the username secret with the static image name string directly
  DOCKER_REPO: ${{ secrets.DOCKER_USERNAME }}/my-node-app 

jobs:
  build-test-deploy:
    runs-on: ubuntu-latest
    
    steps:
    - name: 📦 Checkout Repository
      uses: actions/checkout@v3

    # --- Build and Test Application ---
    - name: 🛠️ Set up Docker BuildX
      uses: docker/setup-buildx-action@v2

    - name: 🏗️ Build Application Image
      # Uses the DOCKER_IMAGE_NAME defined in env
      run: docker build -t ${{ env.DOCKER_IMAGE_NAME }}:latest -f app/Dockerfile .

    - name: 🧪 Run Application Tests
      run: |
        docker run --rm ${{ env.DOCKER_IMAGE_NAME }}:latest npm test

    # --- Minikube Deployment within DinD ---
    - name: 🐳 Build Minikube-in-DinD Image
      run: docker build -t minikube-dind:latest -f minikube-dind/Dockerfile .

    - name: 🚀 Start Minikube-in-DinD Service
      run: |
        # Start the DinD container
        docker run -d --privileged --name minikube-ci-env minikube-dind:latest
        
        # Wait for Minikube's Kubernetes API to be available inside the container
        docker exec minikube-ci-env bash -c '
          until kubectl get nodes >/dev/null 2>&1; do
            echo "Waiting for Minikube Kubernetes API..."
            sleep 5
          done
          echo "Minikube Kubernetes API is ready and configured!"
        '

    - name: 📤 Load Application Image into Minikube's Docker Daemon
      # This is required to use 'imagePullPolicy: Never' in the deployment
      run: |
        # Save the application image to a tarball and load it into the DinD container's Docker daemon
        docker save ${{ env.DOCKER_IMAGE_NAME }}:latest | docker exec -i minikube-ci-env docker load

    - name: 🚢 Deploy Application to Minikube
      run: |
        # Create a simple Kubernetes deployment YAML
        cat <<EOF > deployment.yaml
        apiVersion: apps/v1
        kind: Deployment
        metadata:
          name: my-node-app-deployment
        spec:
          replicas: 1
          selector:
            matchLabels:
              app: my-node-app
          template:
            metadata:
              labels:
                app: my-node-app
            spec:
              containers:
              - name: my-node-app
                image: ${{ env.DOCKER_IMAGE_NAME }}:latest
                imagePullPolicy: Never # Crucial: tells Kubernetes to use the locally loaded image
                ports:
                - containerPort: 3000
        ---
        apiVersion: v1
        kind: Service
        metadata:
          name: my-node-app-service
        spec:
          selector:
            app: my-node-app
          ports:
            - protocol: TCP
              port: 80
              targetPort: 3000
          type: NodePort
        EOF
        
        # Copy the deployment YAML into the DinD container and apply it using the internal kubectl
        docker cp deployment.yaml minikube-ci-env:/tmp/deployment.yaml
        docker exec minikube-ci-env kubectl apply -f /tmp/deployment.yaml
        
        # Verify deployment (optional, but good for CI)
        docker exec minikube-ci-env kubectl rollout status deployment/my-node-app-deployment --timeout=5m

    # --- Push to Docker Hub ---
    - name: 🔑 Log in to Docker Hub
      uses: docker/login-action@v2
      with:
        username: ${{ env.DOCKER_USERNAME }}
        password: ${{ env.DOCKER_PASSWORD }}

    - name: 🏷️ Tag and Push Application Image to Docker Hub
      # Tag the image with the full repo path and push
